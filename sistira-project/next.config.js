/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
    output: 'standalone',
    basePath: '/sistira',
    assetPrefix: '/sistira/',
};

module.exports = nextConfig;
  